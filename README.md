# Раскрой из спецификации

Автоматизация раскроя линейного проката: спецификация Компас → расчёт раскроя.

## Что где

```
raskroy/
├── raskroy.py            ← главный скрипт: запуск всего процесса
├── sp_to_cutlist.py      ← конвертер спецификации Компас → формат калькулятора
├── requirements.txt      ← список библиотек (с версиями)
├── README.md             ← этот файл
├── ИНСТРУКЦИЯ.md          ← подробная установка с нуля
│
├── smart_cut_app/        ← калькулятор раскроя (с GitHub Nivoronkov/smart_cut_app)
│   ├── materials_catalog.json   ← справочник склада (ведётся вручную)
│   └── leftovers_db.json        ← справочник остатков
│
└── tests/                ← тесты (защита от регрессий)
    ├── test_razbor.py
    └── data/karkas_etalon.xls   ← эталонная спецификация для теста
```

## Быстрый старт

```cmd
cd C:\raskroy
venv\Scripts\activate
python raskroy.py "спецификация.xls"
```

Результат — два файла рядом со спецификацией:
- `..._вход.xlsx` — что система поняла (для проверки глазами);
- `..._раскрой.xlsx` — готовая карта раскроя.

## Как работать с файлом «_вход» (светофор)

Лист «Детали» — живой:
- вверху плашка-вердикт: «✗ ЗАПОЛНИТЕ красные строки» или «✓ ВСЁ ЗАПОЛНЕНО»;
- красные строки — нет длины или кода материала, их надо заполнить;
- персиковые строки — деталь с доработкой (_д): режется как обычно, но
  проверь рез под углом / доп. обработку;
- зелёные строки — всё заполнено.

Правишь ячейки прямо в книге — вердикт и цвета пересчитываются сразу.
Когда вердикт зелёный — запусти `raskroy.py` ещё раз, он посчитает.

**Важно:** если `..._вход.xlsx` уже существует, повторный запуск берёт
ТВОИ правки (не перетирает). Если поля не заполнены — расчёт блокируется
и скрипт скажет, какие строки доделать. Чтобы пересоздать «_вход» заново
из спецификации — удали файл «_вход.xlsx».

## Прогон тестов

После любой правки конвертера:
```cmd
venv\Scripts\activate
pytest
```
Зелёно — разбор эталона не сломан. Красно — правка что-то поломала.

## Подробности

Полная установка с нуля — в `ИНСТРУКЦИЯ.md`.
