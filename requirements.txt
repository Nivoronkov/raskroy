# Библиотеки для обработки спецификаций и расчёта раскроя.
# Установка: pip install -r requirements.txt
#
# Версии зафиксированы намеренно — чтобы установка на другой машине
# или через год собралась так же, как сегодня.

pandas==3.0.2
openpyxl==3.1.5
xlrd==2.0.2

# для запуска тестов (pytest)
pytest==9.0.3

# PySide6 нужен ТОЛЬКО для окна калькулятора (smart_cut_app/main.py).
# Если работаешь только через raskroy.py (командой) — можно не ставить.
PySide6==6.8.1.1
